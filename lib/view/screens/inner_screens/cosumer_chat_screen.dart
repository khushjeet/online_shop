import 'package:flutter/material.dart';

class ConsumerChatScreen extends StatelessWidget {
  const ConsumerChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          "Chat Screen",
        ),
      ),
    );
  }
}
